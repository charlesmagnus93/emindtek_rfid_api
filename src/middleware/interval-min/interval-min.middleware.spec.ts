import { IntervalMinMiddleware } from './interval-min.middleware';

describe('IntervalMinMiddleware', () => {
  it('should be defined', () => {
    expect(new IntervalMinMiddleware()).toBeDefined();
  });
});
